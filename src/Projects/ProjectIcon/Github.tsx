import React from 'react';
import {
	Github
} from 'react-bootstrap-icons';

function GitHubIcon() {
    return (
        <Github
            color='rgb(0, 0, 0)'
        />
    );
}

export default GitHubIcon;
